#ifndef ZEPHYR_FS_H
#define ZEPHYR_FS_H

/* Stub: the Zephyr filesystem layer is replaced by in-RAM stores in the
 * ESP-IDF port. This header exists only so legacy includes still resolve. */
static inline int zephyr_fs_init(void) { return 0; }

#endif
